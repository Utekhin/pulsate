{
  "name": "wave-circle-sound-game",
  "version": "1.0.0",
  "description": "Interactive sound visualization game",
  "type": "module",
  "scripts": {
    "start": "PATH=$PATH:./node_modules/.bin && serve ./ -p $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
